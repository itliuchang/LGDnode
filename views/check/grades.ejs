{{ include ../layout/header }}
<link rel="stylesheet" type="text/css" href="/css/check/grades.css">
	<table class="table table-striped">
		<thead>
			<tr>
				<th>科目</th>
				<th>分数</th>
				<th>绩点</th>
			</tr>
		</thead>
		<tbody>
			{{ for(var i = 0;i<data.length;i++){ }}
			<tr>
				<th>{{=data[i].coursename}}</th>
				<th>{{=data[i].grade}}</th>
				<th>{{=data[i].point}}</th>
			</tr>
			{{ } }}
		</tbody>
	</table>
{{ include ../layout/footer}}
